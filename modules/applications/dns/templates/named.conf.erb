options {
	directory "/etc/named";

	listen-on { <%= dns_ListenOn %> };

	forwarders { <%= dns_forwarders %> };
};

<% dns_zones.each do |zone| -%>
zone "<%= zone %>" IN {
	type master;
	file "<%= zone %>";
    notify yes;
    allow-update { <%= dns_allowUpdate %> };
};
<% end -%>
